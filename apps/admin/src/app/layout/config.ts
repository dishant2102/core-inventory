import { PATH_DASHBOARD } from '../routes/paths';


export const PATH_AFTER_LOGIN = PATH_DASHBOARD.root; // as '/dashboard/app'
export const SPACING = 16;
export const BREADCRUMB_HEIGHT = 50;
export const HEADER = {
    H_MOBILE: 64,
    H_DESKTOP: 64,
};

export const NAV = {
    W_VERTICAL: 280,
    W_MINI: 88,
};
