/**
 * Auth module exports (Refactored to use shared package)
 */

export { authClient } from './auth-client';
export { SecureStorageAdapter } from './storage-adapter';

// Re-export types if needed from @libs/react-shared or @libs/types
// export type { User } from '@libs/types';
